# Generated by init-fleet.sh. Edit as needed post-init.
# See https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-security/customizing-your-repository/about-code-owners
#
# Default owner is the org/user itself — resolves for both user-owned and
# org-owned repos. For org adopters, tighten this to a team (e.g.
# `@${github_org}/platform-engineers`) once the team exists.
*   @${github_org}
